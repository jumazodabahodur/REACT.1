import React from 'react'

const Button = ({name}) => {
  return (
    <div>
<button className='rounded-[10px] bg-[black] !text-[white] md:p-[20px]'>{name}</button>

    </div>
  )
}

export default Button
